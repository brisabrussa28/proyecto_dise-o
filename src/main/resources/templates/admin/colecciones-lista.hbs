{{#partial "nav_izquierda"}}
    <a href="/admin/dashboard" class="btn btn-outline-light btn-sm d-flex align-items-center gap-2"
       title="Volver al Mapa">
        <i class="fa-solid fa-arrow-left"></i>
        <span class="d-none d-sm-block">Volver</span>
    </a>
{{/partial}}

{{#partial "contenido"}}
    <div class="container py-4">

        <div class="row mb-4">
            <div class="col-12">
                <h2 class="fw-bold"><i class="fa-solid fa-layer-group me-2"></i>Gestión de Colecciones</h2>
                <p class="text-muted">Agrupa hechos, define fuentes y aplica algoritmos de consenso.</p>
            </div>
        </div>

        <div class="row">

            <div class="col-md-4">
                <div class="card shadow-sm mb-4">
                    <div class="card-header bg-primary text-white fw-bold">
                        <i class="fa-solid fa-plus me-2"></i>Nueva Colección
                    </div>
                    <div class="card-body">
                        <form action="/admin/colecciones" method="POST">
                            <div class="mb-3">
                                <label class="form-label fw-bold">Nombre</label>
                                <input type="text" name="nombre" class="form-control"
                                       placeholder="Ej: Reportes de Tránsito 2025" required>
                                <div class="form-text">Podrás configurar la fuente y el algoritmo en el siguiente
                                    paso.
                                </div>
                            </div>
                            <button type="submit" class="btn btn-primary w-100">Crear Colección</button>
                        </form>
                    </div>
                </div>

                <div class="alert alert-info small">
                    <i class="fa-solid fa-circle-info me-1"></i>
                    <strong>Recuerda:</strong> Una colección necesita una <em>Fuente</em> de datos y un
                    <em>Algoritmo</em> para poder generar verdad consensuada.
                </div>
            </div>

            <div class="col-md-8">
                <div class="card shadow-sm">
                    <div class="card-header bg-white fw-bold">Colecciones Existentes</div>
                    <div class="card-body p-0">
                        <div class="list-group list-group-flush">

                            {{#each colecciones}}
                                <div class="list-group-item d-flex justify-content-between align-items-center p-3">

                                    <div>
                                        <h5 class="mb-1 fw-bold text-dark">{{this.nombre}}</h5>

                                        <div class="small text-muted mb-1">
                                            <i class="fa-solid fa-database me-1"></i>
                                            {{#if this.fuente}}
                                                Fuente: {{this.fuente.nombre}}
                                            {{else}}
                                                <span class="text-danger">Sin fuente asignada</span>
                                            {{/if}}
                                        </div>

                                        <div>
                                            <i class="fa-solid fa-filter me-1"></i>
                                            {{#if this.algoritmo}}
                                                <span class="badge bg-info text-dark">{{this.algoritmo.class.simpleName}}</span>
                                            {{else}}
                                                <span class="badge bg-warning text-dark">Sin algoritmo</span>
                                            {{/if}}

                                            {{#if this.hechos}}
                                                <span class="badge bg-secondary ms-1">{{this.hechos.size}} hechos cacheados</span>
                                            {{/if}}
                                        </div>
                                    </div>

                                    <div class="d-flex gap-2">
                                        <a href="/admin/colecciones/{{this.id}}" class="btn btn-outline-primary btn-sm">
                                            <i class="fa-solid fa-gear me-1"></i> Configurar
                                        </a>
                                    </div>

                                </div>
                            {{else}}
                                <div class="text-center py-5">
                                    <i class="fa-solid fa-folder-open fa-3x text-muted mb-3 opacity-50"></i>
                                    <p class="text-muted">No hay colecciones creadas todavía.</p>
                                </div>
                            {{/each}}

                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
{{/partial}}

{{> templates/layout}}